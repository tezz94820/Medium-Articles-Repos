import React from 'react'

type BookProps = {
    title: string;
    summary: string;
}

const Book = ({title, summary}:BookProps) => {
    console.log(title,summary);
  return (
    
  )
}

export default Book